TODO

* Thorough check of current addon_modules.py/functions
    - sanitizing
    - sensible logging via log()
* re-connect on pinged out
* write a README
* fix logging
     - raw_log and log with file and stdout options
* better core modules
* move db inits inside relevant functions
* Fully port spewing quotes from file based to DB based
* A poll function, to cater voting needs
    -topic
    -index based voting
    -tracking votes
    -add users "idea/vote" to poll if user votes for new
* A function for self updating bhottu
* Commit needs some checking and more options?
